{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for clipboard-pro example app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "clipboard-pro:default"
  ]
}
